<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematron="http://download.ros.org/schema/package_format3.xsd"?>
<package format="3">
  <name>turtle_py</name>
  <version>0.0.0</version>
  <description>turtlesim distance publisher/subscriber and square driver</description>
  <maintainer email="jje5976@gmail.com">조은</maintainer>
  <license>Apache-2.0</license>

  <depend>rclpy</depend>
  <depend>std_msgs</depend>
  <depend>std_srvs</depend>
  <depend>geometry_msgs</depend>
  <depend>tf2_ros</depend>
  <depend>visualization_msgs</depend>
  <depend>rcl_interfaces</depend>
  <depend>action_msgs</depend>
  <!-- 문제 6: 커스텀 인터페이스(Waypoint/WaypointList/DrawPolygon)를 쓰기 위한 의존.
       colcon 은 이 <depend> 를 보고 turtle_interfaces 를 turtle_py 보다 먼저 빌드한다(문제 8). -->
  <depend>turtle_interfaces</depend>
  <exec_depend>turtlesim</exec_depend>
  <!-- 문제 9: launch 파일 실행에 필요 -->
  <exec_depend>launch</exec_depend>
  <exec_depend>launch_ros</exec_depend>

  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
